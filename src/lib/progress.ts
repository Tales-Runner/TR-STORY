/**
 * 읽기 진행률(0~1 fraction) 임계값 — 여기 한 곳으로 모은다.
 *
 * 이전엔 0.02 / 0.05 / 0.8 / 0.95 / 0.99 리터럴이 selectors·hooks·컴포넌트
 * 6곳에 흩어져 있었고 상한도 불일치(이어보기 0.95 vs 인라인 바 0.99)했다.
 * 값 자체는 그대로 두되(동작 불변), 각 임계값의 "의미"에 이름을 붙여 산재를
 * 없앤다.
 */
export const PROGRESS = {
  /** 이 아래는 노이즈/“시작 안 함”으로 취급(진행률 저장·표시 하한). */
  NOISE_FLOOR: 0.02,
  /** “이어 보기” 후보로 치는 상한(이 위는 사실상 다 봄). */
  CONTINUE_MAX: 0.95,
  /** 메인 카드 하단 인라인 진행률 바의 상한(의도적으로 CONTINUE_MAX 보다 높음). */
  BAR_MAX: 0.99,
  /** 뷰어 재진입 시 스크롤 위치를 복원하는 구간. */
  RESUME_MIN: 0.05,
  RESUME_MAX: 0.95,
  /** 자동 “읽음” 마킹 임계값. */
  READ_MARK: 0.8,
} as const;
